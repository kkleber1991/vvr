#Instalação de dependências  
dependencies:
  composer:
    - install
  npm:
    - install

#Especificando como gerar a key
run:
  - command: cp .env.example .env
  - command: ls -la  # Verificar se o arquivo .env foi copiado corretamente
  - command: php artisan key:generate

#Configurar .env
env:
  APP_KEY: ${APP_KEY}
  APP_URL: http://localhost
  DB_CONNECTION: mysql   # Corrigido aqui
  DB_HOST: mysql
  DB_PORT: 3306
  DB_DATABASE: ksvitrinevirtualdb
  DB_USERNAME: admvitrinevirtualdb
  DB_PASSWORD: PWzG&v8tQs6z

